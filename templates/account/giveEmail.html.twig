{% extends 'base.html.twig' %}

{% block title %}Saisie d'un email.{% endblock %}

{% block body %}
    <div class="container bg-primary py-3 px-3">
        <h1>Saisie votre email.</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-success">Saisir votre email.</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
